A TauCFixedPipe is a pipe with a max size. It does not implement a fixed buffer size, but it maintain the buffer inside a max size.

Instance Variables
	size:		<Object>

size
	- The maximum size of the buffer.
