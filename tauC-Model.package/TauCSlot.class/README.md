A TauCSlot is a place in a pipe. It is reified as an object to be able to track it.

Instance Variables
	pipe:		<Object>
	slot:		<Object>

pipe
	- The pipe it is connected to

slot
	- the value it contains
